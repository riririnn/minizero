## デバッグ用トレーニングコマンド
`./tools/quick-run.sh train shogi az 2 -n shogi_9x9_az_test -conf_file configs/shogi.cfg  `


# 🐛 デバッグ記録：Optimizationフェーズ開始直後のクラッシュ問題

## 1. 初期症状と問題の切り分け
* **症状:** SelfPlayは正常に完了しSGFファイルも生成されるが、最適化（Optimization）フェーズに入った途端にプログラムが終了（Training failed to complete）する。
* **デバッグログによる切り分け:**
  `zero_server.cpp` にワーカーへ送信するコマンド（`train weight_iter_0.pkl 1 1`）を出力するログを追加。
  👉 **結果:** サーバーは正常にコマンドを発行している。問題は**コマンドを受け取った後のPythonワーカー（`train.py`）側にある**ことが確定。

## 2. クラッシュの種類と真のエラーメッセージの特定
* **症状の詳細:** サーバー側の `connections_` ループに `std::cout` を仕込んで検証した結果、C++サーバーが落ちているのではなく、Pythonのプロセスが `Aborted (core dumped)` で強制終了していることが判明。
* **ログ（`op.log`）の確認:**
  Python側のエラーログを確認すると、以下のC++（PyBind裏側）の例外が発生していた。
  1. `std::length_error: cannot create std::vector larger than max_size()`
  2. （修正後）`std::bad_alloc`（メモリ確保失敗）
  👉 **推測:** C++のデータ読み込みモジュール（DataLoader）が、将棋の巨大なデータ（Policyサイズ: 11,259）を処理する際、サイズ計算を誤って天文学的なサイズの配列（Vector）を作ろうとしてメモリがパンクしている。

## 3. プリントデバッグによる「犯行現場」の絞り込み
GDBを使わず、`print` や `std::cerr` を使って境界線を絞り込んだ。

* **パイプ通信の罠:**
  最初、C++側に `std::cout` や Pythonに通常の `print()` を仕込んだところ、**「標準出力」がサーバーとの通信パイプとして使われていたため**、サーバーが誤作動を起こした（`Worker Error`）。
  👉 **解決策:** デバッグ出力はすべて**標準エラー出力（`std::cerr` / `sys.stderr`）**に流すことで、通信を邪魔せずにログを取得できるようになった。

## 4. 決定的な証拠の発見（バトンの確認）
エラーが「SGFファイルの読み込み時」なのか「サンプリング時」なのかを特定するため、`train.py` と `data_loader.cpp` にデバッグログを配置。

* **ログの出力結果:**
  `[DEBUG] 今から読み込むファイル: 1.sgf`
  `[Python] DataLoaderの呼び出しが無事終わりました`
  `[DEBUG] 1. C++のサンプリング(sample_data)を呼び出します`
  `terminate called after throwing an instance of 'std::length_error'`
  
  👉 **事実発覚:** `1.sgf` の**読み込み（load_data）自体はエラーなく成功**していた。しかし、データをランダムに抽出する**サンプリング処理に入った瞬間に爆発**していた。

## 5. 根本原因（真犯人）の特定
「なぜサンプリングで爆発するのか？」を調べるため、C++側に「もし学習用データ（ReplayBuffer）が空っぽだったら停止する」という安全装置（`[FATAL]`エラー）を導入。

* **判明したカラクリ:**
  1. プログラムは `1.sgf` を読み込んだ。
  2. しかし、SGFのフォーマットが合わず、データが弾かれて**破棄された**。
  3. 学習データが**空っぽ（0件）**のままサンプリング処理へ突入した。
  4. C++が空の配列の0番目に無理やりアクセスし、そこにあった「ゴミデータ（マイナス値など）」を配列サイズだと勘違いして巨大なVectorを作ろうとし、自爆💥。

* **なぜSGFデータが破棄されたのか？（将棋特有の問題）**
  エラーで吐き出されたSGFの中身（例: `B[10339]`）を確認したことで原因が完全に判明した。
  既存の `SGFLoader` は囲碁・オセロ向けに作られており、**「アクションはアルファベット2文字（例: `B[ab]`）」**という前提でパースしていた。
  将棋の `10339` のような数字（ID）が来ると「不正な文字列」とみなし、勝手に `PASS` に変換して環境（Env）に渡し、将棋ルール上あり得ない手として弾かれていた。さらに、将棋用のカスタムパーサー（`shogi.cpp`）がSGF形式を想定しておらず、データを読み飛ばしていた。

## 6. 最終的な修正
MiniZeroのC++コードおよび起動スクリプトに対し、以下の4つの修正を施した。

1. **`minizero/utils/sgf_loader.cpp` の修正:**
   SGFタグの値が「数字」から始まる場合（将棋のアクションID）は、2文字アルファベットへの変換を通さず、そのままの数字文字列としてアクションリストに追加するよう `if` 文を追加。
2. **`minizero/environment/shogi/shogi.cpp` の修正:**
   データが `(` から始まる場合（SGF形式の場合）は、人間用パーサーではなく、標準のパーサー（`BaseBoardEnvLoader`）に読み込みを任せるようにした。
3. **`minizero/environment/shogi/shogi.h` の修正:**
   読み込んだ数字のアクションIDを、しっかり `action_id_` として保存するようにコンストラクタを修正。
4. **`scripts/zero-worker.sh` の修正:**
   `Job_Optimization` の処理で、Python（`train.py`）を起動するコマンドが `#` でコメントアウトされていたため、これを解除して正しく起動するようにした（これがないと `read format error` が発生する）。

これにより、SGFが正しくパースされて ReplayBuffer に格納されるようになり、無事に Optimization（ネットワークの学習）フェーズが開始できるようになった。